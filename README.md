# Smart Appointment Scheduling System 🗓️

## 1. شرح عام عن المشروع (Project Description)
**نظام إدارة المواعيد الذكي** هو تطبيق Backend متطور مبني باستخدام تقنية **Spring Boot**. يهدف النظام إلى حل مشكلات تنظيم المواعيد التقليدية عبر توفير واجهات برمجية (RESTful APIs) قوية.

### مميزات النظام:
* إدارة متكاملة للمستخدمين، الخدمات، وأوقات العمل.
* منع التعارضات الزمنية (Double Booking).
* نظام صلاحيات مبني على الأدوار (Roles):
    * **Admin:** صلاحيات كاملة لإدارة النظام والخدمات والموافقة على المواعيد.
    * **Staff:** الاطلاع على المواعيد الخاصة والمخصصة له.
    * **Customer:** حجز المواعيد وتعديلها أو إلغاؤها.

---

## 2. فريق العمل وتقسيم المهام (Team & Task Distribution)
*محمد حسني محمد اسامه مراد 
بكري بكرو حسنو 
احمد حسن النبهان

**

> **ملاحظة:** تم إنجاز الوظيفة بشكل تعاوني خلال عدة جلسات تقنية في الكلية بحضور كافة أعضاء الفريق.

---

## 3. كيفية تشغيل المشروع (How to Run)
اتبع الخطوات التالية لتشغيل المشروع في بيئتك المحلية:

1.  **تحميل المشروع:** قم بعمل `Clone` للمستودع أو تحميله كملف المضغوط.
2.  **قاعدة البيانات:** قم بإنشاء قاعدة بيانات (MySQL/PostgreSQL) وتحديث الإعدادات في ملف `application.properties`.
3.  **بناء المشروع:** استخدم أداة Maven لبناء المشروع عبر الأمر:
    ```bash
    mvn clean install
    ```
4.  **التشغيل:** قم بتشغيل التطبيق باستخدام الأمر:
    ```bash
    mvn spring-boot:run
    ```

---

## 4. توثيق الـ APIs (API Documentation)

###  إدارة المستخدمين (Authentication & Users)
| Method | Endpoint | Description |
| :--- | :--- | :--- |
| POST | `/api/auth/register` | تسجيل مستخدم جديد |
| POST | `/api/auth/login` | تسجيل الدخول والحصول على JWT |
| GET | `/api/users` | عرض جميع المستخدمين (Admin فقط) |
| GET | `/api/users/{id}` | عرض تفاصيل مستخدم محدد |
| PUT | `/api/users/{id}` | تعديل بيانات مستخدم |
| DELETE | `/api/users/{id}` | حذف مستخدم |

###  إدارة الخدمات (Services)
| Method | Endpoint | Description |
| :--- | :--- | :--- |
| POST | `/api/services` | إضافة خدمة جديدة |
| GET | `/api/services` | عرض جميع الخدمات |
| GET | `/api/services/{id}` | عرض تفاصيل خدمة محددة |
| PUT | `/api/services/{id}` | تعديل خدمة |
| DELETE | `/api/services/{id}` | حذف خدمة |

### أوقات العمل (Working Schedule)
| Method | Endpoint | Description |
| :--- | :--- | :--- |
| POST | `/api/schedules` | إضافة وقت عمل جديد |
| GET | `/api/schedules` | عرض جميع أوقات العمل |
| DELETE | `/api/schedules/{id}` | حذف وقت عمل |

###  إدارة المواعيد (Appointments)
| Method | Endpoint | Description |
| :--- | :--- | :--- |
| POST | `/api/appointments` | حجز موعد جديد |
| GET | `/api/appointments` | عرض جميع المواعيد |
| GET | `/api/appointments/{id}` | عرض تفاصيل موعد محدد |
| PUT | `/api/appointments/{id}` | تعديل موعد |
| DELETE | `/api/appointments/{id}` | إلغاء موعد |
| PUT | `/api/appointments/{id}/status` | تغيير حالة الموعد (مقبول/مرفوض) |
